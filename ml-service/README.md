# ML Service (future)

This directory will contain the Python / FastAPI-based ML service that hosts NLP preprocessing and the spam detection model.

Planned contents:
- `app/` - FastAPI application
- `models/` - trained model artifacts
- `requirements.txt` - Python dependencies
- `Dockerfile` - optional containerization

Do NOT implement ML in this repo root yet. This is a placeholder for future work.
